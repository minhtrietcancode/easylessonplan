services:
  backend:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # Mount your current directory for development
      - .:/app
    environment:
      - PYTHONPATH=/app
    # Override the CMD for development - keeps container running
    command: tail -f /dev/null